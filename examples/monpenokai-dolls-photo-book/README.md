# Monpenokai Dolls Photo Book Promo

Expected engine: `hyperframes`

Input type: URL / Web photo book promo

Use this to validate that a public web page promo with browser mockups, DOM/page visuals, and deterministic card-style motion routes to HyperFrames while still showing relative guidance for Remotion and Editframe.

```bash
pnpm michibiki decide \
  --prompt "$(cat examples/monpenokai-dolls-photo-book/prompt.txt)"
```

Generation, preview, and render remain separate approval-gated stages.
